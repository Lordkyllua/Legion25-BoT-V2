const { PermissionsBitField } = require('discord.js'); module.exports={name:'mute',description:'Mute member',async execute(message){ if(!message.member.permissions.has(PermissionsBitField.Flags.ModerateMembers)) return message.reply('No permission'); const m=message.mentions.members.first(); if(!m) return message.reply('Mention member'); let role=message.guild.roles.cache.find(r=>r.name==='Muted'); if(!role) role=await message.guild.roles.create({ name:'Muted', permissions:[] }); await m.roles.add(role); message.reply('Member muted.'); }};