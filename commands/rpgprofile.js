const rpg=require('../utils/rpg'); module.exports={name:'rpgprofile',description:'Show rpg profile',execute(message){ const u=rpg.getUserData(message.author.id); message.reply(`${message.author.username} — L${u.level} XP:${u.xp} Gold:${u.gold}`); }};