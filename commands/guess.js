module.exports={name:'guess',description:'Guess number',execute(message,args){ const n=parseInt(args[0]); if(isNaN(n)) return message.reply('Usage: !guess <1-10>'); const t=Math.floor(Math.random()*10)+1; message.reply(n===t?`Correct! I picked ${t}`:`Wrong. I picked ${t}`); }};