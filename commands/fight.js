const rpg=require('../utils/rpg'); module.exports={name:'fight',description:'Fight',execute(message){ const gain=Math.floor(Math.random()*30)+10; const u=rpg.getUserData(message.author.id); u.gold += gain; rpg.saveUserData(message.author.id,u); message.reply(`You fought and gained ${gain} gold.`); }};