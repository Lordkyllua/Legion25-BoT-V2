const fetch=require('node-fetch'); module.exports={name:'quote',description:'Random quote',async execute(message){ try{ const r=await fetch('https://api.quotable.io/random'); const d=await r.json(); message.reply(`"${d.content}" — ${d.author}`); }catch(e){ message.reply('Error'); } }};