const fetch=require('node-fetch'); module.exports={name:'meme',description:'Random meme',async execute(message){ try{ const r=await fetch('https://meme-api.com/gimme'); const d=await r.json(); message.channel.send({ content:d.title, files:[d.url] }); }catch(e){ message.reply('Error'); } }};