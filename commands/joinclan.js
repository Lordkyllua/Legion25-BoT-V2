const clans=require('../utils/clans'); module.exports={name:'joinclan',description:'Join clan',execute(message,args){ const name=args.join(' '); if(!name) return message.reply('Usage: !joinclan <name>'); const r=clans.joinClan(message.author.id,name); if(!r.ok) return message.reply('Error'); message.reply('Joined clan'); }};